{
  "name": "examples",
  "targets": {
    "run-example": {
      "command": "tsx --tsconfig {projectRoot}/tsconfig.json {projectRoot}/{args.example}/host.ts",
      "dependsOn": ["^build"]
    }
  }
}
